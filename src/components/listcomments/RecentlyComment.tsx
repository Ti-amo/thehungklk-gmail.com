import React from 'react'
import { isMobile } from 'react-device-detect'
import styled from 'styled-components'

interface Props {
    
}

const RecentlyComment = (props: Props) => {
    return (
        <h1>dgdfgfd</h1>
    )
}

export default RecentlyComment
